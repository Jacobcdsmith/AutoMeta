# Production Environment Configuration

# API Endpoints (proxied through nginx)
VITE_LLM_GATEWAY_URL=/api/llm
VITE_PUPPETEER_URL=/api/puppeteer
VITE_MCP_URL=/api/mcp

# Production mode
NODE_ENV=production
VITE_ENV=production
